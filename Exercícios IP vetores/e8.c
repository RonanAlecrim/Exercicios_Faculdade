#include <stdio.h>

int main(){
    int n, contador = 0, t_novo = 0;
    scanf("%d", &n);
    
    int vet[n];

    for (int i = 0; i < n; i++)
    {
        scanf("%d", &vet[i]);
    }

    

    printf("%d", contador);

    return 0;
}
